import React from 'react';
import Button from '../../components/Button/Button';
import Header from '../../components/Header/Header';

const Home = () => {
  return (
    <div>
      <Header>
        <h1>Welcome to rounting!</h1>
        <Button/>
      </Header>
    </div>
  );
};

export default Home;